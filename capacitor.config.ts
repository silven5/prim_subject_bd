import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prim_subject_bd',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
